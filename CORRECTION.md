# v3 输入错误与 v4 修正

v3 的权重加载、训练执行和逐题指标检查都不足以发现错误分词。本项目没有验证输入编码与预训练模型的分词资产一致，导致遗留加载入口在当前环境中产生 0 条 BPE 合并规则；英文退化为字符分割。旧数字作为故障记录保留，撤回正常 RoBERTa/LoRA 效果解释。

`tokenization_audit.json` 给出直接证据：旧入口 0 条 merges，新入口 50,000 条；固定训练数据的 3,391/3,668 句对由截断变为 0/3,668。新入口逐项等于 `tokenizers.Tokenizer.from_file` 从原始文件生成的全部 4,076 条编码，并通过文档中的 `Hello world → [0,31414,232,2]` 探针。

四轮修正实验沿用 v3 数据划分、种子、优化器、学习率、批量和精度；主要干预是修正输入路径。额外 12 轮实验单独运行，用于观察预算作用；它与 4 轮使用不同长度的预定学习率计划，不能把长程训练的前四轮当作四轮实验。MRPC 验证结果曾被查看，扩展明确列为事后探索。PAWS 压力测试仅在 MRPC 内部开发选优后评价，不用于训练或调参。

这是本项目的加载选择与检查遗漏，不以未经调查的上游库问题代替责任。v4 新增输入一致性检查及缓存来源检查，保留原始记录和更正过程。仍不宣称复现原论文全部结果表。
